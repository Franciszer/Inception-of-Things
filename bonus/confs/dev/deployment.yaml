apiVersion: apps/v1
kind: Deployment
metadata:
  name: bonus-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bonus-app
  template:
    metadata:
      labels:
        app: bonus-app
    spec:
      containers:
      - name: bonus-app
        image: wil42/playground:v1
        ports:
        - containerPort: 8888
